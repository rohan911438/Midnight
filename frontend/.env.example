NEXT_PUBLIC_NETWORK_ID=preview
# Filled in automatically by ../scripts/sync-frontend-zk-assets.mjs from the
# root .env's CONTRACT_ADDRESS (run via predev/prebuild) -- this file only
# documents the shape for a from-scratch/manual setup.
NEXT_PUBLIC_CONTRACT_ADDRESS=
